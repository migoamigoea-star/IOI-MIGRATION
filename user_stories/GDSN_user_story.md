# User Story — Gift/Donation/Sponsorship Notification (`GDSN`)

> **Department:** HR  
> **Module:** Compliance — Gifts and Hospitality  
> **Site(s):** PRAI, JOHOR  
> **SharePoint List:** `MainDB_HR **Form Discriminator:** FormCode = "GDSN"`  
> **Form Code:** `GDSN`

---

## 1. App Overview & Purpose

GDSN records all gift receipts, donations made, and sponsorships given or received by IOI Acidchem
staff, ensuring compliance with the company's anti-bribery and anti-corruption policy. Each
notification is submitted by the relevant employee and approved by their HOD. The migrated solution
must preserve item-level declaration details, SAP reference linkage, total value computation, and
HOD attribution for audit evidence.

---

---

## 2. User Stories

**US-01: Create & Submit**
> As a **Employee** (member of `D05-HR-Staff`),  
> I want to **create & submit** in the `GDSN` application,  
> So that the workflow advances to the next approval stage.  
>
> *Trigger:* When Status='Draft' and item created

**US-02: HOD Approve / Reject**
> As a **HOD** (member of `D05-HR-HOD`),  
> I want to **hod approve / reject** in the `GDSN` application,  
> So that the workflow advances to the next approval stage.  
>
> *Trigger:* When Status='Submitted' — notify HOD via adaptive card

**US-03: Close**
> As a **System** (member of `—`),  
> I want to **close** in the `GDSN` application,  
> So that the workflow advances to the next approval stage.  
>
> *Trigger:* When HOD decides → set IsLocked=Yes, notify submitter

**US-LIST: Search and filter Gift/Donation/Sponsorship Notification records**
> As an **authorized user**,  
> I want to search, filter, and view Gift/Donation/Sponsorship Notification records in the list screen,  
> So that I can quickly find the record I need to act on.

**US-NOTIFY: Receive workflow notifications**
> As a **workflow participant**,  
> I want to receive email notifications when the form status changes or requires my action,  
> So that I am always aware of pending tasks without checking the app manually.

**US-AUDIT: View audit trail and history**
> As an **Admin / Manager**,  
> I want to view a full audit trail of every status change and approver action,  
> So that I can meet compliance and traceability requirements.

---

## 3. SharePoint List Requirements

**Target List:** `MainDB_HR **Form Discriminator:** FormCode = "GDSN"`

| #   | SP Internal Name | Display Label         | Column Type         | Required | Classification   | Source Mapping / Notes                               |
| --- | ---------------- | --------------------- | ------------------- | -------- | ---------------- | ---------------------------------------------------- |
| 1   | Title            | Title                 | Single line text    | Yes      | SYSTEM-COMPUTED  | GDSN prefix + auto-sequence number                   |
| 2   | FormCode         | Form Code             | Single line text    | Yes      | SYSTEM-COMPUTED  | Fixed value GDSN                                     |
| 3   | GDSNNo           | GDSN No               | Single line text    | No       | SYSTEM-COMPUTED  | Auto-generated by Power Automate on submit           |
| 4   | ProjectCode      | Project Code          | Single line text    | No       | USER-ENTERED     | `ProjectCode` — SAP project reference                |
| 5   | ReqType          | Request Type          | Choice              | Yes      | USER-ENTERED     | Gift, Donation, Sponsorship                          |
| 6   | Company          | Company               | Choice              | Yes      | USER-ENTERED     | `company` — IOI Oleochemical, IOI Acidchem           |
| 7   | RecipientName    | Recipient Name        | Single line text    | Yes      | USER-ENTERED     | `Recipient` / `Name`                                 |
| 8   | RecipientAddress | Recipient Address     | Multiple lines text | No       | USER-ENTERED     | `Address`                                            |
| 9   | SAP_ID           | SAP ID / Vendor Code  | Single line text    | No       | USER-ENTERED     | `SAP_ID`                                             |
| 10  | RequestorName    | Requestor             | Person or Group     | Yes      | USER-ENTERED     | `Requestor`                                          |
| 11  | RequestorDept    | Department            | Single line text    | Yes      | USER-ENTERED     | `Department`                                         |
| 12  | ItemType         | Item Type             | Choice              | Yes      | USER-ENTERED     | `Type` — Cash, Voucher, Product, Hamper, Other       |
| 13  | Purpose          | Purpose               | Multiple lines text | Yes      | USER-ENTERED     | `Purpose`                                            |
| 14  | ItemDetail       | Item Description      | Multiple lines text | Yes      | USER-ENTERED     | `ItemDetail`                                         |
| 15  | DateReceived     | Date Given / Received | Date and Time       | Yes      | USER-ENTERED     | `DateRec`                                            |
| 16  | Quantity         | Quantity              | Number              | Yes      | USER-ENTERED     | `Quantity`                                           |
| 17  | UnitPrice        | Unit Price (RM)       | Currency            | Yes      | USER-ENTERED     | `Price`                                              |
| 18  | TotalValue       | Total Value (RM)      | Currency            | No       | SYSTEM-COMPUTED  | `Total` = Quantity × UnitPrice                       |
| 19  | IsPO             | Purchase Order Raised | Yes/No              | No       | USER-ENTERED     | `IsPO`                                               |
| 20  | PONumber         | PO Number             | Single line text    | No       | USER-ENTERED     | `PoNo` — only if IsPO = Yes                          |
| 21  | HOD              | Head of Department    | Person or Group     | Yes      | USER-ENTERED     | `HOD`                                                |
| 22  | HODApprovalDate  | HOD Approval Date     | Date and Time       | No       | SYSTEM-COMPUTED  | `HODDate` — stamped when HOD approves                |
| 23  | HODRemarks       | HOD Remarks           | Multiple lines text | No       | USER-ENTERED     | HOD approval comments                                |
| 24  | CurrentStatus    | Current Status        | Choice              | Yes      | WORKFLOW-MANAGED | Draft, Submitted, HOD_Approved, Rejected             |
| 25  | WorkflowStage    | Workflow Stage        | Number              | Yes      | WORKFLOW-MANAGED | 1=Draft 2=Submitted 3=HOD_Review 4=Approved/Rejected |
| 26  | CurrentAction    | Current Action        | Choice              | Yes      | WORKFLOW-MANAGED | Hidden routing field                                 |
| 27  | EnvironmentTag   | Environment           | Choice              | Yes      | SYSTEM-COMPUTED  | DEV, TEST, PROD                                      |
| 28  | IsLocked         | Is Locked             | Yes/No              | No       | WORKFLOW-MANAGED | True after HOD decision                              |



---

## 4. Screen Requirements

| Screen | Purpose | Visible To |
|--------|---------|------------|
| GDSN_List | Gallery | List all GDSN records with status and type filter |
| GDSN_New | Form | New notification: item details + recipient + value |
| GDSN_View | Read-only | View full notification record |
| GDSN_Edit | Form | Edit draft before submission |
| GDSN_HODApproval | Approval | HOD decision: Approve / Reject with remarks |

### Screen Interaction Details

**GDSN_List Screen**
- Gallery displaying all `GDSN` records from `MainDB_HR **Form Discriminator:** FormCode = "GDSN"`.
- Search box filters by `Title` and `Status`.
- Status badge shows colour-coded current state (Draft = grey, Submitted = blue, Approved = green, Rejected = red).
- `+ New` button navigates to `GDSN_New` (visible to Initiator role only).
- Tap a row to navigate to `GDSN_View`.

**GDSN_New / _Edit Screen**
- Data entry form bound to `MainDB_HR **Form Discriminator:** FormCode = "GDSN"`.
- Required fields highlighted in red when empty.
- `Save as Draft` button: patches record with `Status = Draft`.
- `Submit` button: disabled until all required fields are filled; on press patches `Status = Submitted` and triggers the Submit flow.
- `Cancel` button: discards changes and navigates back.

**GDSN_View Screen**
- All fields displayed in read-only mode.
- `Edit` button visible to Initiator when `Status = Draft` or `Returned`.
- `Approve` / `Reject` buttons visible to Approver role when `Status = Submitted` or `InApproval`.

---

## 5. Formula Requirements (Power Fx)

The following Power Fx formulas must be implemented in the Canvas App:

### 1. Title / Record ID Auto-Generation

```powerfx
// On form open for new record, set a unique display ID
Set(varTitle, "GDSN-" & Text(Now(), "YYYYMMDD-HHMMSS"))
```

### 3. Field Lock Based on Status

```powerfx
// Lock all editable fields once record is Approved or Closed
DisplayMode: If(
    ThisItem.Status in ["Approved", "Closed", "Archived"],
    DisplayMode.View,
    DisplayMode.Edit
)
```

### 4. Screen Navigation

```powerfx
// Navigate from List to New form
Navigate(GDSN_New, ScreenTransition.Slide)

// Navigate from List to View screen for selected record
Navigate(GDSN_View, ScreenTransition.None, {varRecord: ThisItem})

// Save (Patch) and navigate back to list
Patch(MainDB, {
    Title: varTitle,
    FormCode: "GDSN",
    Status: "Draft",
    SubmittedBy: User(),
    SubmittedDate: Now()
});
Navigate(GDSN_List, ScreenTransition.Back)
```

### 5. List Screen Search & Filter

```powerfx
// Gallery Items formula — filter by FormCode and search text
Filter(
    MainDB,
    FormCode = "GDSN" &&
    (IsBlank(txtSearch.Text) ||
     StartsWith(Title, txtSearch.Text) ||
     StartsWith(Status, txtSearch.Text))
)
```


---

## 6. Business Logic Requirements

The following business rules and logic must be enforced:

### 1. Status Transition Rules

Status must only progress through the defined workflow stages in sequence:
- **Stage 1:** `Create & Submit` — performed by `Employee`
- **Stage 2:** `HOD Approve / Reject` — performed by `HOD`
- **Stage 3:** `Close` — performed by `System`

> ⚠️ **Status must never be changed directly by end-users.** All status transitions are managed exclusively by Power Automate flows.

### 2. Role-Based Access Control

| Role | Allowed Actions |
|------|----------------|
| All staff | D05-HR-Staff |
| HOD group | D05-HR-HOD |
| HR Manager | D05-HR-Manager |

### 3. Data Integrity Rules

- `FormCode` must always equal `GDSN` (system-enforced constant).
- `SubmittedBy` and `SubmittedDate` are system-stamped on first submission and must not be editable.
- `ApprovedBy` and `ApprovedDate` are set by the flow only when all required approvals are collected.
- Records in `Approved`, `Closed`, or `Archived` state must be fully read-only.
- `EnvironmentTag` must be set from `Config_AppSettings` and must match the deployment environment (DEV / TEST / PROD).

### 4. Notification Logic

- Notifications are sent via Power Automate (email / Teams adaptive card).
- Notification recipients are determined by the role matrix and workflow stage.
- All notifications must include: Record Title, Current Status, Action Required (if any), and deep-link URL to the record.

### 5. Audit & Traceability

- Every status change must be logged in the SharePoint item version history.
- Multi-stage approvals must write individual approval records to `cr_approvalrecord` child list (if applicable).
- Rejection comments must be captured and returned to the initiator.


---

## 7. Power Automate Requirements

| Flow Name           | Trigger                                  | Action                                                                            |
| ------------------- | ---------------------------------------- | --------------------------------------------------------------------------------- |
| HR_GDSN_OnSubmit    | When Status='Draft' → item submitted     | Generate GDSNNo, compute TotalValue = Qty × Price, stamp date, notify HOD         |
| HR_GDSN_HODDecision | When Status='Submitted' and HOD responds | If Approve: set CurrentStatus=HOD_Approved, IsLocked=Yes, notify submitter        |
|                     |                                          | If Reject: set CurrentStatus=Rejected, IsLocked=Yes, notify submitter with reason |

### Flow Design Principles

All Power Automate flows for this application must adhere to the following:

1. **Environment isolation:** Flows must read environment-specific settings (email recipients, approver groups, retry limits) from `Config_AppSettings` using the `EnvironmentTag` field. Never hard-code DEV/TEST/PROD-specific values.
2. **Idempotency:** Flows must handle duplicate triggers gracefully (do-until loops with condition checks).
3. **Error handling:** All HTTP actions and SharePoint calls must be wrapped in try/catch scope with failure notifications to the admin group.
4. **Status locking:** Only flows may update the `Status` and `CurrentAction` fields. Canvas App patches to these fields are blocked via SharePoint column validation or item-level permissions.
5. **Audit stamping:** Every flow must write a version comment to the SharePoint item history when changing status.
6. **Notification standard:** All emails / Teams cards must include: Record Title, Form Code, Current Status, Action Required, and a deep-link to the record in the Canvas App.

---

## 8. Acceptance Criteria

The following acceptance criteria must be met before this form can be promoted to PROD:

- [ ] All SharePoint columns in `MainDB_HR **Form Discriminator:** FormCode = "GDSN"` are created with correct types and required flags.
- [ ] Canvas App screens (`GDSN_List`, `GDSN_New`, `GDSN_View`, `GDSN_Edit`) are functional.
- [ ] All required field validations prevent submission of incomplete forms.
- [ ] Status field is read-only in the Canvas App; transitions are flow-only.
- [ ] All Power Automate flows are tested end-to-end in TEST environment.
- [ ] Notification emails are received by correct recipients at each stage.
- [ ] Approved records are fully locked (no edits possible).
- [ ] Role-based visibility is enforced: Initiators cannot approve their own records.
- [ ] Audit trail is complete and immutable for all status transitions.
- [ ] `EnvironmentTag` correctly isolates DEV / TEST / PROD data.
